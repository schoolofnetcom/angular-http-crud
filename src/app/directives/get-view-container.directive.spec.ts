import { GetViewContainerDirective } from './get-view-container.directive';

describe('GetViewContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new GetViewContainerDirective();
    expect(directive).toBeTruthy();
  });
});
